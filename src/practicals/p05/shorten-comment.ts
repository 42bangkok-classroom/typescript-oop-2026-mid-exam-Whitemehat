const comment = "TypeScript is a strongly typed programming language"

function shortenComment(comment: string): string {
  // Write your code below
  let n = comment.length

  while(x === ""){
    let word = "";
    for(const w of comment){
      if(w !== " "){
        word += w;
      }
    }
    if(word){

    }
  }
}
//const result = shortenComment("TypeScript is a strongly typed programming language");
console.log(result);